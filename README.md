# Yamibo Scraper（百合会小说抓取工具）

一个用于抓取百合会论坛连载帖内容的小工具，可导出为 **TXT** 或 **EPUB**，便于离线阅读。

## 免责声明与版权说明

1. 本项目仅用于学习与技术交流。
2. 抓取内容版权归原作者及百合会论坛所有。
3. 请仅用于个人阅读，不要二次传播或用于商业用途。
4. 使用者应自行承担使用风险与责任。

## 环境准备

项目使用 [uv](https://github.com/astral-sh/uv) 管理依赖。

### 1) 安装 uv

```bash
pip install uv
```

### 2) 安装依赖

```bash
uv sync
```

## 配置方式（统一文件管理）

程序会把配置存到项目根目录的 `yamibo_config.json`，包括：

- `user_agent`
- `cookie`
- `username`
- `password`
- `book_title` / `book_author`
- `raw_html_catalog`

首次运行可直接通过 CLI 输入，输入后会自动保存。后续需要修改时，在菜单里选择 **“修改配置”** 即可。

### raw_html_catalog 推荐录入方式

在“修改配置 -> 7. raw_html_catalog”里支持三种方式：

1. 单行粘贴
2. 多行粘贴（`END` 单独一行结束）
3. 从本地文件读取

程序会提示“字符长度 + 检测到的链接数”，方便你判断是否读入成功。

## 运行

- `auth.py`：登录与会话初始化
- `search.py`：关键词搜索帖子
- `cli.py`：终端交互
- `config_store.py`：本地配置加载与保存

输出文件默认保存在 `output/` 目录。

## 功能进度

### 已实现功能

- [x] 支持账号密码登录抓取流程（同时保留 Cookie 登录作为兼容方案）
- [x] 支持关键词搜索并展示可选帖子列表
- [x] 支持从 `raw_html_catalog` 抓取并导出 TXT / EPUB
- [x] 支持通过 CLI 配置并持久化保存到 `yamibo_config.json`

### 开发中 / 待完善

- [ ] 搜索后正文抓取仍不稳定：目前能显示帖子目录页，但尚未成功抓取到正确正文内容（当前判断为解析算法问题，暂时搁置，后续修复）
- [ ] CLI 抓取来源优化：将“关键词搜索抓取”和“按现有 HTML 抓取”拆分为两个独立入口
- [ ] 搜索交互优化：当当前展示的帖子不符合预期时，可继续向下翻页搜索直到满意，并支持随时按 `q` 退出

## CLI 使用说明（当前）

运行后流程：

1. 显示终端编码提示（中文乱码排查）
2. 主菜单：
   - 开始抓取
   - 修改配置
   - 退出
3. 开始抓取时再选择：
   - 输出格式（TXT / EPUB / 同时）
   - 登录方式（账号密码 / Cookie）
   - 抓取来源（`raw_html_catalog` / 关键词搜索）

## 常见问题

### 1) 为什么还有 Cookie 登录？

账号密码登录是默认推荐方式；保留 Cookie 模式是为了兼容验证码、风控或页面结构变化等情况。

### 2) 中文会不会乱码？

大多数 UTF-8 终端都正常。如果出现乱码：

- Windows PowerShell 可先执行：`chcp 65001`
- 确认终端字体支持中文
